get:
  tags:
    - Purchase orders
  summary: Get
  description: Returns detailed information for purchase order with given ID.
  parameters:
    - name: id
      in: path
      description: ID of purchase order
      required: true
      schema:
        type: integer
        format: int64
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: integer
                example: 200
              duration:
                type: number
                example: 0.0531
              data:
                type: object
                properties:
                  PurchaseOrder:
                    $ref: '../schemas/PurchaseOrder.yaml'
                  PurchaseOrderRow:
                    type: array
                    items:
                      $ref: '../schemas/PurchaseOrderRow.yaml'
    400:
      $ref: '../schemas/error400.yaml'
    401:
      $ref: '../schemas/error401.yaml'
    403:
      $ref: '../schemas/error403.yaml'
    429:
      $ref: '../schemas/error429.yaml'
