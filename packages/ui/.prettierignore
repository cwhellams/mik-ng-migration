node_modules
dist
build
*.min.js

# Written by this package's CI job, one step before `pnpm format:check` runs in
# the same directory (`eslint . --format json > eslint-results.json`). Prettier
# reads the .prettierignore next to its cwd, so the repo-root one that already
# lists this file is not in scope here — without this line the lint step makes
# the format step fail. Same reason apps/admin and apps/frontend each carry one.
**/eslint-results.json

# Generated by `pnpm test:coverage`, and gitignored, but for the same cwd reason
# the root .gitignore is not in scope either.
coverage
