node_modules
dist
build
*.min.js

# Written by this package's CI job, one step before `pnpm format:check` runs in
# the same directory. Prettier reads the .prettierignore next to its cwd, so the
# repo-root one that already lists this file is not in scope here. Every package
# with its own CI job carries one of these.
**/eslint-results.json
