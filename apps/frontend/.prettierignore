node_modules
dist
build
*.min.js
*.sql
.env
**/eslint-results.json
# Generated by `pnpm test:coverage`. Gitignored, but Prettier reads the
# .gitignore next to its cwd — the repo-root one that lists `coverage` is not
# in scope here, so `pnpm format:check` would fail on 700+ generated HTML files
# for anyone who has checked the ratchet locally before pushing.
coverage