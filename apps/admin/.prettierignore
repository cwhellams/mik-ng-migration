node_modules
dist
build
*.min.js
*.sql
.env

# Written by this app's CI job, one step before `pnpm format:check` runs in the
# same directory (`eslint . --format json > eslint-results.json`). Prettier
# reads the .prettierignore next to its cwd, so the repo-root one that already
# lists this file is not in scope here — without this line the lint step makes
# the format step fail.
**/eslint-results.json

# Generated by `pnpm test:coverage`. Gitignored, but for the same
# cwd reason the root .gitignore is not in scope, so `pnpm format:check` would
# fail on hundreds of generated HTML files for anyone who has checked the
# ratchet locally before pushing.
coverage
